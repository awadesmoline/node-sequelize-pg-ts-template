# This file is for unifying the coding style for different editors and IDEs.
# More information at http://editorconfig.org
# Popular IDEs have plugins (Sublime, Atom, Eclipse)

# EditorConfig plugins look for a file named .editorconfig in the directory of the
# opened file and in every parent directory. A search for .editorconfig files will

# Stop if the root filepath is reached or an EditorConfig file with root=true is found.
root = true

[*]
end_of_line = lf
charset = utf-8
insert_final_newline = true
indent_style = space
indent_size = 2
